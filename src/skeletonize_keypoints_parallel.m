function skeletonize_keypoints_parallel(path, out_path)
    if ~exist(path, 'dir')
        display('Invalid input folder');
    end
    
    if ~exist(out_path, 'dir')
        display('Invalid output folder');
    end
    
    
end